import React, {Fragment} from 'react';
import './styles.scss';

function Select(props){
    return (
        <select>
            <option value="user1">
                User 1
            </option>   
        </select>
    )
}

export default Select;